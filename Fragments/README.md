# Fragments

残页 / 零散片段：还没成体系、但想公开的短笔记。一篇一个 `.md`。

```yaml
---
title: 时域卷积定理的一个例题
date: 2026-09-10
tags: [信号与系统]
---
```

规则：

- **必须有 `date`**，否则不会出现在归档的「碎片」列表里；
- 想升级成体系化的内容时，把它挪进 `Projects/<新册>/` 并按编号改名（`01-…`、`第一章-…`）即可；
- 图片同样放该碎片旁边的 `img/` 里，正文用 `![[图片名.png]]` 引用。
